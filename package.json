{
  "name": "automata-playground",
  "version": "1.0.0",
  "description": "A web-based visualizer and simulator for finite state machines, pushdown automata, and Turing machines.",
  "author": {
    "name": "Shreyan C",
    "email": "shreyan.chaubey@gmail.com"
  },
  "main": "electron/main.js",
  "devDependencies": {
    "clean-css": "^5.3.3",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^5.4.7",
    "terser": "^5.49.0",
    "vite": "^7.3.1",
    "wait-on": "^8.0.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "electron:dev": "concurrently -k -s first \"npm:dev\" \"wait-on http://localhost:5173 && cross-env ELECTRON_DEV_SERVER_URL=http://localhost:5173 electron .\"",
    "electron:preview": "cross-env SKIP_OBFUSCATION=1 npm run build && electron .",
    "electron:build": "cross-env SKIP_OBFUSCATION=1 npm run build && electron-builder"
  },
  "build": {
    "appId": "com.automataplayground.app",
    "productName": "Automata Playground",
    "files": [
      "electron/**/*",
      "dist/**/*",
      "package.json"
    ],
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "win": {
      "target": ["nsis"]
    },
    "mac": {
      "target": ["dmg"],
      "category": "public.app-category.education"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Education"
    }
  }
}
